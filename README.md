# runcommand

# clone from github
git clone https://github.com/asauravsuman/runcommand.git

# add node library
npm install

# start application
npm start

# endpoint (can change port from server.js)
localhost:3000